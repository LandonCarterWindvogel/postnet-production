// Placeholder for nav destinations that don't have a module yet
// (Stock, Settings). Swap these out sprint by sprint.

export function renderComingSoon(title) {
  return `<section class="page-heading"><div><p class="eyebrow">Coming later</p><h1>${title}</h1><p>This module arrives in a later sprint.</p></div></section>`;
}
